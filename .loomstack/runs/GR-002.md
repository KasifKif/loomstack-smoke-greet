---
task_id: GR-002
runner: aider
model: qwen3-coder-next
endpoint: http://192.168.0.200:8081/v1
status: in_progress
---

Warning: Input is not a terminal (fd=0).

Aider v0.86.2
Model: openai/qwen3-coder-next with whole edit format
Git repo: .git with 7 files
Repo-map: using 1024 tokens, auto refresh
Added .loomstack/context/research/GR-002.md to the chat.
Added CLAUDE.md to the chat.

I'll summarize common pytest patterns for testing single-function modules into 
the specified file.



.loomstack/context/research/GR-002.md

````diff
@@ -1,52 +1,12 @@
-Pytest, a popular testing framework for Python, employs a set of conventions 
for test discovery and supports various patterns to write effective and 
maintainable tests.
+Pytest offers concise patterns ideal for testing single-function modules:
 
-### Test Discovery Patterns
-
-Pytest automatically discovers tests based on the following standard 
conventions:
-*   **File Naming** Pytest looks for files named `test_*.py` or `*_test.py` in 
the current directory and its subdirectories.
-*   **Directory Recursion** It recursively traverses directories, unless they 
match patterns specified in `norecursedirs`.
-*   **Function and Method Naming** Within identified test files, functions and 
methods prefixed with `test_` are collected as test items. For instance, `def 
test_example_function():` would be discovered.
-*   **Class Naming** Tests can be grouped into classes. Pytest collects test 
functions or methods within classes that are prefixed with `Test` and do not 
have an `__init__` method. There's no need to subclass `unittest.TestCase` 
unless specifically desired. For example:
-    ```python
-    class TestMyFeature:
-        def test_something(self):
-            assert True
-    ```
-*   **Python Packages** For more complex project structures, adding 
`__init__.py` files to test directories allows pytest to discover tests as part 
of a package, which can resolve issues with identical module names in different 
locations.
-
-### Common Pytest Test Patterns
-
-Beyond basic discovery, pytest facilitates several powerful testing patterns:
-
-*   **Assertions** Pytest uses standard Python `assert` statements for 
validating test outcomes. This includes using operators like `==`, `!=`, `<=`, 
`>=`, and the `in` operator for checking membership in collections.
-*   **Fixtures** Fixtures are functions designed to set up pre-conditions for 
tests (e.g., creating test data, initializing objects, configuring application 
state) and can also handle teardown processes. They promote reusability and help
maintain a clean test environment.
-    *   **Parametrized Fixtures** Fixtures can be parametrized to provide 
different sets of data to tests, allowing a single test function to run with 
multiple configurations without code duplication.
-    *   **Factory Fixtures** These fixtures dynamically generate test objects 
or data based on specific conditions, enhancing flexibility and reducing 
redundancy, especially when complex object variations are needed.
-*   **Parametrization (`pytest.mark.parametrize`)** This marker allows a single
test function to be executed multiple times with different input values, defined
directly within the test function or through fixtures. This significantly 
reduces redundant test code and improves readability.
-    ```python
-    import pytest
-
-    @pytest.mark.parametrize("input_a, input_b, expected_output", [
-        (1, 2, 3),
-        (5, 7, 12),
-        (10, 0, 10),
-    ])
-    def test_addition(input_a, input_b, expected_output):
-        assert (input_a + input_b) == expected_output
-    ```
-*   **Expecting Exceptions (`pytest.raises`)** To test scenarios where a 
specific exception is expected, `pytest.raises` is used. The test passes if the 
anticipated exception is raised.
-    ```python
-    import pytest
-
-    def divide_by_zero():
-        1 / 0
-
-    def test_divide_by_zero_raises_exception():
-        with pytest.raises(ZeroDivisionError):
-            divide_by_zero()
-    ```
-*   **Markers** Custom markers (`pytest.mark.your_marker`) can be used to 
categorize tests (e.g., `@pytest.mark.slow`, `@pytest.mark.unit`) for selective 
execution or to apply specific behaviors.
-*   **Temporary Directories and Files** Pytest provides a built-in fixture, 
`tmp_path`, for creating unique temporary directories and files within tests, 
useful for functional tests that involve file system interactions.
-*   **Monkeypatching/Mocking** The `monkeypatch` fixture allows for safely 
patching modules, classes, or environment variables during tests, isolating the 
code under test from its actual dependencies.
-*   **Performance Testing** With plugins like `pytest-benchmark`, specific 
patterns emerge for performance testing, such as separating setup from the 
critical path using fixtures and `benchmark.pedantic`, and parameterizing inputs
to understand performance across different scales.
-*   **Isolation Patterns** For hermetic tests, patterns focus on isolating 
components like HTTP clients, databases, filesystems, and processes to ensure 
tests are self-contained and reliable.+*   **Basic test functions** — Write 
`test_<function_name>()` in `test_*.py` files; pytest auto-discovers them.
+*   **Direct assertions** — Use standard `assert` statements to validate 
outputs (e.g., `assert greet("Alice") == "Hello Alice"`).
+*   **Fixtures for setup** — Use `@pytest.fixture` to create reusable 
setup/teardown (e.g., mock data, environment config).
+*   **Parametrized tests** — Use `@pytest.mark.parametrize` to run the same 
test logic with multiple input/output pairs.
+*   **Exception testing** — Wrap code expected to raise with 
`pytest.raises(ExpectedException)` to assert exceptions.
+*   **Monkeypatching** — Use the `monkeypatch` fixture to temporarily replace 
attributes, environment variables, or functions for isolation.
+*   **Temporary filesystem** — Use `tmp_path` fixture to create isolated temp 
files/directories for file-based tests.
+*   **Simple class-based grouping** — Group related tests in `class 
Test<Feature>` if multiple tests exist for one function.
+*   **Test discovery** — Name test files `test_*.py` or `*_test.py`, and 
functions `test_*` to ensure automatic discovery.
+*   **No boilerplate** — Avoid `unittest.TestCase` subclassing unless needed; 
plain functions and classes work out of the box.
````


Tokens: 1.9k sent, 334 received.
Applied edit to .loomstack/context/research/GR-002.md

---
status: done
exit_code: 0
files_modified: [.loomstack/context/research/GR-002.md]
touched_files: [".gitignore", ".loomstack/context/conventions.md", ".loomstack/context/research/GR-002.md", ".loomstack/ledger.jsonl", ".loomstack/runs/GR-001.md", ".loomstack/runs/GR-002.md", "PLAN.autonomous.md", "app.py"]
token_count: 2234
cost_usd: 0.0
---
